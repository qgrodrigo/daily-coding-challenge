** start of script.js **

function launchFuel(payload) {
  let count = 0;

  while(payload > 1){
    payload = payload / 5;
    count += payload
  }

  return count.toFixed(1);
}

** end of script.js **

