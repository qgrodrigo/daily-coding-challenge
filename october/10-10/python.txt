** start of main.py **

def launch_fuel(payload):
    count = 0
    while(payload > 1):
        payload = payload / 5
        count += payload
        #print(masa)
    return round(count,1)

def prueba(masa):
    count = 0
    while(masa > 1):
        masa = masa / 5
        count += masa
        #print(masa)
    print(round(count,1))

def calcular_combustible(masa):
    combustible_total = 0.0
    incremento = masa / 5

    while(incremento > 1):
        combustible_total += incremento
        masa_total = masa + combustible_total
        nuevo_combustible = masa_total / 5
        incremento = nuevo_combustible - combustible_total
    print(combustible_total)
    #return combustible_total


** end of main.py **

